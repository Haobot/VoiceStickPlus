# 体感鼠标热调参面板说明

## 打开方式

在 VoiceStick Windows 客户端托盘菜单中选择 **“体感鼠标调参（热调参）”** 即可打开。该窗口为非模态窗口，改动即时生效，无需点击“保存”也能立刻作用到当前体感鼠标运动上；点击“保存到配置”后参数会写入 `config.toml`，下次启动依然生效。

## 当前控制模型：角度控制（Angle-to-Velocity）

v1.8.x 采用**角度控制模型**：

- 设备进入体感鼠标模式瞬间的姿态被记为**中立姿态**（零点）。
- 手腕转动后保持某个偏离角度，光标会持续朝该方向移动；角度越大、速度越快。
- 手腕回到中立姿态附近时，光标快速停止。

因此下面的“阈值”参数指的是**偏离角度的大小**（固件上报角速度在桌面端积分后的相对角度），而不是原始角速度。

## 滑杆含义

### 左右灵敏度（1–10）

控制光标在**水平方向（X 轴，yaw）**的整体快慢。

- 内部映射：`gain_x = 档位 × 16`。
- 默认值 5 对应 gain_x = 80。
- 调大：手腕同样角度下光标横向移动更快，适合跨屏或大显示器。
- 调小：横向更慢、更稳，适合精细对位。

### 上下灵敏度（1–10）

控制光标在**垂直方向（Y 轴，pitch）**的整体快慢。

- 内部映射：`gain_y = 档位 × 16`。
- 默认值 5 对应 gain_y = 80。
- 多数用户习惯左右比上下稍快，可单独调节。

### tau 时间常数（0.02–0.50 秒）

速度环一阶低通的时间常数，决定光标速度跟随目标速度的“顺滑程度”以及手停后的滑行距离。

- 默认值 0.05 s。
- 调小（如 0.02）：更跟手、停止更快；但陀螺仪噪声可能导致光标微抖。
- 调大（如 0.10–0.50）：更平滑、噪声被压制；手停后光标会滑行约 `3 × tau` 才完全停住。

### 微调段阈值（1.0–30.0）

三段线性增益曲线的第一个拐点。当偏离角度绝对值小于该阈值时，进入“微调段”，使用较低的微调段增益，便于精确对位。

- 默认值 15.0。
- 调大：更多日常慢转角度被纳入低增益区，整体更“稳”，但大角度响应会变慢。
- 调小：更快进入中段/甩动段，日常操作更“灵”，但精细对位容易冲过头。

### 甩动段阈值（30.0–80.0）

三段线性增益曲线的第二个拐点。当偏离角度绝对值大于该阈值时，进入“甩动段”，使用较高的甩动段增益，便于快速跨屏。

- 默认值 50.0。
- 必须大于“微调段阈值”，程序会自动保证 `微调段阈值 < 甩动段阈值`。
- 调大：需要更大的手腕角度才进入高增益甩动段，中段过渡区变宽，日常操作更线性。
- 调小：轻微甩手腕就进入高增益，跨屏更快，但可能难控制落点。

### 微调段增益（0.05–0.50）

微调段的速度倍率，作用于 `|theta| < 微调段阈值` 时。

- 默认值 0.15。
- 该倍率与灵敏度共同决定微调段速度：`v_target = theta × gain × 微调段增益`。
- 调小：微调段光标极慢，适合精确点击小按钮。
- 调大：微调段变快，日常操作更轻快，但容易过头。

### 甩动段增益（2.0–6.0）

甩动段的速度倍率，作用于 `|theta| ≥ 甩动段阈值` 时。

- 默认值 4.0。
- 调大：大角度下手腕甩动对应光标极速，跨 4K 屏更轻松。
- 调小：甩动段更保守，防止光标飞出屏幕。

### 中立区死区（1.0–10.0）

方向锁的中立区阈值，控制“光标什么时候停”以及“什么时候才能切换方向”。

- 默认值 3.0。
- 当手腕偏离角度 `|theta|` 小于此值时，光标速度强制为 0，同时释放方向锁；
- 只有手腕从中立区出发、偏离超过此值，才会锁定该方向并持续移动；
- 从右向左切换时，必须先回到中立区（光标停下），再重新出发向左，避免回转经过水平时误触发反向移动。
- 调大：中立区更宽，方向切换必须经过更明显的“回中”动作，更稳但切换延迟稍大。
- 调小：中立区更窄，更跟手，但容易在回中时因轻微过冲而误切方向。

### 中段过渡

当 `微调段阈值 ≤ |theta| < 甩动段阈值` 时，增益在“微调段增益”和“甩动段增益”之间线性插值，保证两段拐点处速度连续、无跳变。

### 反转 Y 轴

勾选后，手腕上抬时光标向下移动，下压时光标向上移动。根据个人习惯决定是否勾选。

## 按钮说明

- **保存到配置**：把当前滑杆值写入 `%APPDATA%\VoiceStick\config.toml`，持久化。
- **重置默认**：恢复出厂默认值（灵敏度 5/5、tau 0.05、曲线 15/50/0.15/4.0、不反转 Y）。

## 调参建议

1. **先调灵敏度**：找到日常跨屏和精细对位都舒服的横向/纵向整体速度。
2. **再调 tau**：感觉光标“飘”或“停不住”时先调 tau；tau 越小越跟手、越快停。
3. **然后调中立区死区**：左右切换时容易误反向 → 调大；感觉回中后光标反应慢、切换拖沓 → 调小。
4. **最后调曲线**：
   - 觉得慢转容易冲过头 → 调大“微调段阈值”或调小“微调段增益”。
   - 觉得甩手腕跨屏不够快 → 调小“甩动段阈值”或调大“甩动段增益”。
   - 觉得日常转动速度跳跃明显 → 拉大两个阈值的间距，让中段过渡更宽。

## 参数范围与默认值

| 参数 | 范围 | 默认值 | 说明 |
|---|---|---|---|
| 左右灵敏度 | 1–10 | 5 | `gain_x = 灵敏度 × 16` |
| 上下灵敏度 | 1–10 | 5 | `gain_y = 灵敏度 × 16` |
| tau | 0.02–0.50 | 0.05 | 速度环时间常数（秒） |
| 微调段阈值 | 1.0–30.0 | 15.0 | 低增益区上界 |
| 甩动段阈值 | 30.0–80.0 | 50.0 | 高增益区下界 |
| 微调段增益 | 0.05–0.50 | 0.15 | 低增益区倍率 |
| 甩动段增益 | 2.0–6.0 | 4.0 | 高增益区倍率 |
| 中立区死区 | 1.0–10.0 | 3.0 | 方向锁中立区阈值，死区内光标停 |
| 反转 Y 轴 | 开/关 | 关 | 垂直方向反向 |

## 相关实现

- 核心算法：`desktop/windows/src/air_mouse_kin.{h,cc}`
- 热调参窗口：`desktop/windows/src/air_mouse_tuning_window.{h,cc}`
- 协调器集成：`desktop/windows/src/voice_stick_coordinator.{h,cc}`
- 设计方案：`Doc/Plan/air-mouse-angle-control.md`、`Doc/Plan/air-mouse-gain-curve.md`
